<%- include('../components/header',{title:"Login-Laundry",auth:auth,data:data,admin:admin}) %>



  <div class='body'>
    <div class='LoginBox glow'>
      <div class='te'>
        <form  method="post" action="/login" onsubmit="return validateform()">
          <h2>Login</h2>
          <div class='InputBox'>
            <input name="roll" onblur="validateroll()" required>
            <label id="titl">Roll</label>
            <p id="emailerror" style="color: red;"></p>

          </div>
          <div class='InputBox spac'>
            <input name="password" onblur="validatepassword()" type="password" required>
            <label id="titl">Password</label>
            <p id="passworderror" style="color: red;"></p>

          </div>
          <button  type='submit' class="my-2 ">Login</button>
          <div class='RegisterLink'>
            <p>Don't have an account ? <a href="/Register" class='Reg'>Register</a></p>
          </div>
        </form>
      </div>
    </div>
  </div>

  <% if(condition){ %>
    <div id="myModal" class="modal1">
      <div class="modal-content1"><div id="close">&times;</div>
        <p>User with same Roll number already exists please login</p>
      </div>
    </div>
    <% } %>
      <!-- <% if(login){ %>
        <div id="myModal" class="modal1">
          <div class="modal-content1"><div id="close">&times;</div>
            <p>Please login first</p>
          </div>
        </div> -->
        <!-- <% } %> -->
          <% if(password1){ %>
            <div id="myModal" class="modal1" >
              <div class="modal-content1"><div id="close">&times;</div >
                <p>Password Wraang Try again</p>
              </div>
            </div>
            <% } %>
              <% if(success){ %>
                <div id="myModal" class="modal1" >
                  <div class="modal-content1"><div id="close" >&times;</div>
                    <p>Sucessfully registered please login</p>
                  </div>
                </div>
                <% } %>

                  <style>
                    .modal1 {
                      display: block;
                      position: fixed;
                      z-index: 10;
                      padding-top: 100px;
                      left: 0;
                      top: 0;
                      width: 100%;
                      height: 100%;
                      overflow: auto;
                      background-color: rgb(0, 0, 0);
                      background-color: rgba(0, 0, 0, 0.4);
                    }

                    /* Modal Content */
                    .modal-content1 {
                      background-color: #fefefe;
                      margin: auto;
                      padding: 20px;
                      border: 1px solid #888;
                      width: 80%;
                    }

                    /* The Close Button */
                    #close {
                      color: #aaaaaa;
                      float: right;
                      font-size: 28px;
                      font-weight: bold;
                    }

                  #close:hover,
                    #close:focus {
                      color: #000;
                      text-decoration: none;
                      cursor: pointer;
                    }



                    #login {
                      padding-left: 25%;
                      padding-right: 25%;
                    }

                    .error {
                      border: solid 3px red;
                    }
                  </style>
                  <script>
                    console.log("hello")
                    function clicked(){
                      console.log("hello")
                    }
                    var modal = document.getElementById("myModal");
                    var span = document.getElementById("close");
                    span.onclick = function close () {
                      console.log("he;;o")
                      modal.style.display = "none";
                    }

                    window.onclick = function (event) {
                      if (event.target == modal) {
                        modal.style.display = "none";
                      }
                    }

                    function validateemail() {
                      if (($("#email").val()).length >= 1) {
                        $("#emailerror").text("")
                        $("#email").removeClass("error")
                      }
                      if ($("#email").val() == "") {
                        $("#email").addClass("error")
                        $("#emailerror").text("Roll No Cannot Be Empty")
                      }
                    }
                    function validatepassword() {
                      if (($("#password").val()).length >= 1) {
                        $("#passworderror").text("")
                        $("#password").removeClass("error")
                      }
                      if ($("#password").val() == "") {
                        $("#password").addClass("error")
                        $("#passworderror").text("Roll No Cannot Be Empty")
                      }
                    }


                    function validateform() {
                      var isValid = true;
                      if ($("#email").val() == "") {
                        isValid = false;
                        $("#email").addClass("error")
                        $("#emailerror").text("Roll No Cannot Be Empty")
                      }
                      if ($("#password").val() == "") {
                        isValid = false;
                        $("#password").addClass("error")
                        $("#passworderror").text("Password Cannot Be Empty")
                      }

                      return isValid
                    }
                  </script>
                 
                  <%- include('../components/footer') %>